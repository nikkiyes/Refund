# Railway deployment configuration

[build]
builder = "RAILPACK"

[build.railpackConfig]
pythonVersion = "3.11"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
