# Railway deployment configuration
# This file tells Railway how to build and run the bot.

[build]
builder = "RAILPACK"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
